//imports
import { ErrorCard } from "@/components/auth/ErrorCard";

//6. Setting-up-auth-error-page
const AuthErrorPage = () => {
    return (
        <ErrorCard />
    )
}

export default AuthErrorPage;
//now, you can view "admin", "server" & "client" inside (protected) separately, I think and we will be done, InshaAllah!!